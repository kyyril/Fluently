const lucide = require('./node_modules/lucide-react-native');
console.log(Object.keys(lucide).filter(k => k.toLowerCase().includes('mail') || k.toLowerCase().includes('lock') || k.toLowerCase().includes('eye')));
